# TFN Data

This folder stores persistent data. Donut delete it, and make sure it exists on your server or these features will not work

The following files will only be created if this folder exists:
- `coteries.json` Coterie memberships, portraits, etc
- `persistent_phones.json` Reserved phone numbers
